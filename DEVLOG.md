# 半导体器件物理 · 刷题 开发日志

## 项目简介

半导体器件物理课程的刷题网页，支持选择题和翻转卡片两种学习模式。  
分离式 HTML + CSS + JS + JSON 题库，纯前端，无需服务器，浏览器直接打开即可使用。

## 技术栈

- **语言**：HTML + CSS + JavaScript（三文件分离，无框架）
- **存储**：`localStorage`（学习进度、XP、成就等）
- **题库**：`questions.json`（约 300 道题，418KB）
- **兼容**：PC + 手机端响应式

## 文件结构

```
semiconductor_quiz/
├── index.html        ← HTML 结构
├── style.css         ← CSS（展开、按组件分组、含注释）
├── app.js            ← JavaScript 逻辑（按功能分区）
├── questions.json    ← 题库（只读）
└── DEVLOG.md         ← 本文件
```

## 数据模型（localStorage）

键名 `semiquiz_data`，JSON 结构：

```json
{
  "answered": {
    "题目ID": {
      "selected": 2,          // 选中的选项索引
      "correct": true,        // 是否正确
      "timestamp": 1700000000000,
      "wrongCount": 1,        // 累计错误次数
      "selfEval": "easy"      // 自评：easy / medium / hard
    }
  },
  "wrongList": [1, 5, 23],    // 错题 ID 列表
  "xp": 150,
  "level": 2,
  "achievements": { "first": 1700000000000 },
  "studyLog": {
    "2026-05-21": { "count": 10, "correct": 7, "wrong": 3, "time": 0 }
  }
}
```

主题偏好单独存为 `localStorage` 键 `semiquiz_theme`（`"day"` / `"night"`）。

## 核心功能

| 功能 | 说明 |
|------|------|
| 选择题模式 | 四选一，选完显示解析 |
| 翻转卡片模式 | 先看题，点翻转看答案，自评 |
| 章节筛选 | 按 12 个章节过滤题目 |
| 三种刷题模式 | 顺序 / 随机 / 错题优先 |
| 搜索 | 按题目内容或解析搜索 |
| 学习统计 | 进度条、正确率、XP 经验值、等级 |
| 成就系统 | 10 个成就（答题数、连续天数、等级等） |
| 学习日历 | 月度学习日历 + 连续天数 |
| 错题本 | 记录错题，可点击跳转 |
| 今日总结 | 今日答题数、正确率、学习时长 |
| 主题切换 | 白天模式 / 夜晚护眼模式 |
| 数据导入导出 | 导出 JSON 备份，换设备可导入恢复 |
| 键盘快捷键 | ← → 翻题，f 翻转，1-4 选选项 |
| 手机端适配 | 顶栏折叠，响应式布局 |

## 重要函数索引（`app.js`）

| 函数 | 行号（约） | 作用 |
|------|-----------|------|
| `init()` | 210 | 入口：加载数据 → 获取题库 → 渲染 → 显示欢迎页 |
| `loadData()` / `saveData()` | — | 读写 localStorage |
| `applyFilters()` | 285 | 筛选题目（章节 + 模式 + 搜索词） |
| `showQuestion(index)` | — | 显示指定索引的题目 |
| `selectOption(qId, idx)` | — | 选择题选答案 + 判对错 + 加 XP |
| `renderFlip(q)` / `flipCard()` | — | 翻转卡片模式 |
| `selfEval(level)` | — | 自评（会 / 模糊 / 不会） |
| `setMode(m)` | — | 切换 选择题 / 翻转卡片 模式 |
| `showWelcome()` | ~680 | 显示欢迎页 |
| `startLearning()` | ~690 | 点击"开始学习"进入做题 |
| `setTheme(theme)` / `loadTheme()` | ~700 | 主题切换 + 持久化 |
| `exportData()` / `importData()` | — | 数据导入导出 |
| `resetData()` | ~784 | 一键重置：双重确认后清除所有学习数据 |
| `toggleMenu()` | — | 手机端顶栏折叠展开 |
| `toggleSettings()` | — | 打开/关闭设置面板 |
| `updateStats()` | — | 更新进度条、正确率等统计 |
| `spawnConfetti()` | — | 🎉 撒花特效 |

## 最近修改记录

### 2026-06-28（侧栏版本与更新栏）

1. **新增版本信息卡片** - 侧边栏新增“当前版本 / 已有功能 / 此次更新”栏目，展示保研面试题库版版本号、核心功能和最近更新内容。
2. **优化侧栏视觉层次** - 使用半透明渐变卡片、功能标签和紧凑更新列表，让版本信息常驻但不干扰主导航。

### 2026-06-28（恢复原题库卡片内容）

1. **补回原始卡片题干与答案** - 从初始题库按题号恢复 `card_question` 和 `card_answer`，用于翻转卡片模式；选择题仍保留保研面试追问题干、选项和口答要点。
2. **选择题与卡片双轨显示** - 选择题模式继续使用 `question_text/options/short_answer`；翻转卡片正面使用 `card_question`，背面使用 `card_answer`，公式、解析和面试口答要点继续辅助复习。
3. **AI 上下文区分题型** - AI prompt 同时携带选择题题目和原卡片题目/答案，避免把选择题选项当成唯一简答材料。
4. **校验工具加防线** - `tools/validate-question-bank.js` 新增 `card_question/card_answer` 必填检查，防止后续题库重写再次覆盖卡片内容。
5. **AI 解析按模式分轨** - 选择题、随机考试和翻转卡片使用不同 AI 缓存/追问键；翻转卡片模式下的提示词优先围绕 `card_question/card_answer` 生成原题口答框架，只有明确追问选项时才解释选择题判断。

### 2026-06-28（刷题续练 + 面试式 AI 解析 + Markdown 渲染）

1. **新增练习会话续练** - 新增 `semiquiz-practice-session-v1`，保存当前练习标题、题目 ID、题号、模式、翻转状态、答题记录和返回页面；刷新或退出后可从首页及章节/错题/收藏/搜索页继续上次练习。
2. **完成后自动清理临时会话** - 整套练习进入结果页后清除续练入口；如果题库更新导致题目 ID 丢失，恢复时会自动过滤无效题目，避免页面崩溃。
3. **AI 解析改为面试分层输出** - 初次 AI 解析固定要求输出“选择题判断”“面试简答回答”“记忆抓手与易错点”，把选项判断和 30-60 秒口答框架分开。
4. **追问上下文同步升级** - 追问 prompt 单独携带题干、选项、正确选项、简答答案、面试口答要点、公式、原解析和历史追问，便于分别回答“为什么选这个”和“简答怎么记”。
5. **增强本地 Markdown 渲染** - AI 内容支持标题、列表、加粗、斜体、行内代码、代码块、引用、分隔线以及 `$...$`、`$$...$$`、`\(...\)`、`\[...\]` 公式文本样式；所有内容仍先转义 HTML，避免直接注入。

### 2026-06-28（保研面试导向题库改造）

1. **题库升级为面试追问风格** - 300 道题已由 API 辅助改写为保研面试概念追问型题目，重点训练物理图像、公式含义、适用条件和易错辨析。
2. **错误选项重写** - 每题 3 个错误选项改为同题语境下的真实误区，不再随机搬运其他题目的正确答案。
3. **新增面试口答要点** - 每题新增 `interview_points`，用于选择题反馈、翻转卡背面、搜索和 AI 解析上下文。
4. **新增题库生成与校验工具** - `tools/generate-question-bank-ai.js` 支持 API 批量生成、断点缓存、20 次/分钟限速；`tools/validate-question-bank.js` 检查题量、章节、选项、正确答案和同步状态。
5. **密钥安全边界** - API key 只在运行时输入，不写入题库、缓存、备份或 Git 仓库；中间缓存位于 `.cache/` 并由 `.gitignore` 忽略。

### 2026-06-28（题目内 AI 追问 + 自定义提示词）

1. **AI 配置新增自定义回答要求** - 默认要求为“先讲物理图像，再讲公式含义，最后给出考前记忆提示；主动指出近似条件、适用范围和易错点”，可在 AI 配置页自行修改并持久保存。
2. **AI 解析下方新增本题追问** - 每道题的 AI 解析展开后，会显示推荐追问、自由输入框、发送按钮和本题追问历史；推荐追问会先填入输入框，用户仍可修改后再发送。
3. **追问上下文更完整** - 每次追问都会携带当前题目、选项、正确答案、公式、题库解析、用户答案或自评、首次 AI 解析、本题历史追问和自定义回答要求。
4. **追问历史按题目和模型隔离** - 新增 `semiquiz-ai-chats-v1`，按“模型:题目 ID”保存追问线程；不同题目、不同模型不会串线。
5. **备份同步追问数据** - 本地 JSON 和 GitHub Gist 备份新增 AI 追问历史与自定义提示词字段；API key 和 GitHub token 仍不写入备份文件。
6. **使用边界** - 追问只服务当前题目，不做全局聊天窗口；清空本题对话只删除追问历史，不清除首次 AI 解析缓存。

### 2026-06-28（自动备份）

1. **新增自动备份开关** – 数据备份页的 GitHub Gist 同步区域新增“启用自动备份”选项。
2. **新增备份间隔设置** — 可设置自动备份间隔，默认 30 分钟，限制范围为 5 到 1440 分钟。
3. **自动备份状态记录** — 显示上次自动备份时间、下次预计时间和最近状态；缺少 GitHub token 时会跳过并提示。
4. **防重复提交** — 自动备份和手动备份共用同一套 GitHub Gist 写入逻辑，并防止多个备份请求同时运行。
5. **运行范围说明** — 自动备份只在网页打开期间生效，关闭网页后不会继续后台运行。

### 2026-06-27（融合升级 — 左侧分区界面 + AI/备份）

1. **界面重构为左侧分区式** — 借鉴“习思想刷题软件”的信息架构，新增首页、章节刷题、随机考试、错题本、收藏题库、搜索题目、数据备份、AI 配置 8 个入口。
2. **保留半导体学习特色** — 继续支持选择题即时反馈、翻转卡片自评、公式区、解析区、XP 等级、成就、学习日历和今日总结。
3. **新增收藏题库** — 每道题可收藏/取消收藏，收藏页支持按章节集中重练或卡片复习。
4. **新增 AI 解析** — 支持 OpenAI-compatible 接口，配置 API Base、API Key、模型后，可对练习题和考试题生成解析，并缓存结果。
5. **新增数据备份升级** — 支持本地 JSON 导入导出和 GitHub Gist 同步；备份包含学习进度、错题、收藏、XP、成就、AI 配置与缓存，但不包含 API key 和 GitHub token。
6. **题库加载增强** — 新增 `data/questions.js`，页面优先读取全局题库，失败时回退到 `questions.json`，提高本地双击打开与静态部署可靠性。
7. **localStorage v2 数据模型** — 新增 `semiquiz-progress-v2`、`semiquiz-mistakes-v2`、`semiquiz-favorites-v2`、`semiquiz-ai-config-v2`、`semiquiz-ai-cache-v2`、`semiquiz-backup-config-v2`、`semiquiz-theme-v2`，并兼容迁移旧 `semiquiz_data`。
8. **移动端适配** — 小屏幕下左侧导航折叠为抽屉菜单，主内容区保留足够顶部间距，避免按钮和题目文字重叠。

### 2026-05-24（第二次更新 — 代码结构重构）

1. **CSS 分离并展开** — 将压缩在一行的 CSS 移到独立 `style.css`，按组件分组展开（顶栏、进度条、卡片、选项、反馈、翻转卡片、自评、导航、欢迎页、侧边面板、日历、设置面板、响应式、动画等），每个分组加注释，后续改样式直接搜类名即可
2. **JS 分离到独立文件** — 所有 JavaScript 移到 `app.js`，按功能分区（数据持久化、筛选搜索、渲染题目、学习记录、XP 等级、统计、日历、成就、主题、UI 控制、数据管理、快捷键等），同一文件的全局函数保持不变，`onclick` 调用不受影响
3. **`index.html` 精简** — 只保留 HTML 结构，通过 `<link href="style.css">` 和 `<script src="app.js">` 引用外部文件，浏览器直接双击打开照常工作

### 2026-05-24（第一次更新）

1. **修复翻转卡片导航按钮缺失** — 翻转卡片背面（`.flip-card-back`）是 `position: absolute` 定位，覆盖了外层导航按钮。在卡片背面的自评按钮下方新增一组"上一题/下一题"按钮，确保翻转后能继续跳题
2. **新增"一键重置"按钮** — 设置面板的数据管理模块新增红色边框的"一键重置"按钮，连续弹出两次 `confirm()` 确认框防误触，确认后清除所有 localStorage 数据并刷新页面

### 2026-05-21（第二次更新）

1. **夜晚模式大修** — 调高对比度（深色背景 #121212 + 亮白文字 #f0f0f0），大幅提高可读性
2. **全组件主题化** — 选项按钮、反馈框、公式区、成就徽章、自评按钮、日历等所有组件改为 CSS 变量，切换主题时全部跟随变色
3. **新增组件级 CSS 变量** — `--option-bg`、`--option-border`、`--feedback-correct-bg`、`--fb-formula-bg` 等 10+ 个新变量

### 2026-05-21（第一次更新）

1. **顶栏折叠（手机端）** — 手机端默认只显示标题行，点 ☰ 展开控制栏
2. **设置面板** — 点击 ⚙️ 弹出设置面板（章节筛选、刷题模式、主题切换、数据管理）
3. **欢迎页** — 打开页面先显示欢迎页，点"🎯 开始学习"再进入做题
4. **主题切换** — 白天模式 + 夜晚护眼模式，自动保存偏好
5. **配色主题化** — 硬编码颜色改为 CSS 变量，跟随主题切换
6. **侧边栏修复** — 增加底部 padding，解决手机端错题本被遮挡问题
7. **数据导入导出** — 设置面板内可导出/导入学习数据（JSON）

## 配色变量

```css
:root /* 白天模式 */
  --bg: #F0FAF0          /* 页面背景，浅绿 */
  --card: #FFF            /* 卡片背景，白色 */
  --primary: #4CAF50      /* 主色，绿色 */
  --primary-dark: #388E3C /* 主色深 */
  --primary-light: #A5D6A7
  --text: #2E7D32         /* 主要文字 */
  --text2: #81C784        /* 次要文字 */
  --danger: #EF5350       /* 错误红 */
  --gold: #FFD700         /* 金色，XP */
  --border: #ddd          /* 边框 */
  --input-bg: #fff        /* 输入框背景 */
  --card-hover: #F0FFF0   /* 卡片悬停/高亮 */
  --wrong-bg: #FFF5F5     /* 错题背景 */

[data-theme="night"] /* 夜晚护眼模式 */
  --bg: #1a1d23
  --card: #2a2d35
  --primary: #66BB6A
  --text: #e0e0e0
  --text2: #90A4AE
  --border: #444
  --input-bg: #3a3d45
  --card-hover: #353840
  --wrong-bg: #3a2020
```

## 开发注意事项

1. **三文件分离** — `index.html`（结构）+ `style.css`（样式）+ `app.js`（逻辑），加新功能时注意对应文件
2. **CSS 已按组件展开** — 每个组件块有注释分隔，改样式直接搜类名
3. **localStorage 数据迁移** — 修改数据格式时要考虑向后兼容，旧数据不会自动更新
4. **手机端测试** — 改布局后记得用手机或浏览器开发者工具检查
5. **题库太大（418KB）** — `questions.json` 不能直接在编辑器中完整读取，改题库时用搜索定位
6. **emoji 使用** — 界面大量用了 emoji 图标，保持风格一致
7. **浏览器直接打开** — 虽然文件分开了，但双击 `index.html` 仍可正常工作（`file://` 协议支持 `<link>` 和 `<script src>`）
